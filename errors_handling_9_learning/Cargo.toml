[package]
name = "errors_handling_9_learning"
version = "0.1.0"
edition = "2024"

[profile.release]
panic = "unwind" # default value is unwind
debug=false # default value is false

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
